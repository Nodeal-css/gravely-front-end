# gravely-front-end

### Run express
> cd to root directory and type this command:
```node app.js```

### Run pocketbase
> cd to backend folder and type this command: 
```run pocketbase```

### Run minio
> cd to backend folder and type this command: 
```run minio```